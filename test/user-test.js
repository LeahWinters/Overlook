import { expect } from 'chai';
import Hotel from '../src/hotel';

describe('User', () => {
  let hotel;
  let user1;
  // let booking1;
  // let booking2;
  // let
  beforeEach(() => {
    user1 = new User({
      'id': 33,
      'name': 'Rae Wisozk',
    });
    hotel = new Hotel(roomsData, usersData, bookingsData);
  })
})
